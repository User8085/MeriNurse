{
  "name": "merinursea",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node install.js",
    "dev": "npx concurrently --kill-others-on-fail \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
